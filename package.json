{
  "name": "retrace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RETRACE - a 2D top-down sci-fi stealth/puzzle game about echoes of your own failures.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "@types/node": "^26.1.2",
    "openai": "^7.3.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
